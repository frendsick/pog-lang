/*
 * This is multi line comment
 */

// Variable Assignments
char var1 = 'c';              // Literal(Character)
int  var2 = 42;               // Literal(Integer)
str  var3 = "This is String"; // Literal(String)
int  var4 = 34+35 * 4 / 2-15; // Nested Binary Statement

// Combined Compound and Conditional Statements
if ( var4>var2 )
{
  ++a;  // Unary Expression
  while ( condition ) { // While loop
    {
      ; // No Op
    }
    return var_name;  // Return Statement
  }
}
